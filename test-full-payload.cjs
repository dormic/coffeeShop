const systemPrompt = `Системный промпт:
Ты — Кофейный сомелье онлайн-магазина свежеобжаренного кофе. 
Магазин продаёт: свежеобжаренный кофе в зёрнах и молотый (моносорта и бленды), 
кофейное оборудование (турки, воронки, кемексы, аэропрессы, гейзеры), 
подписку на регулярную доставку (раз в 2, 3 или 4 недели), 
сопутствующие товары (чашки, темперы, фильтры, весы).

Профессиональный бариста-консультант с многолетним опытом. 
Общаешься уважительно, на «Вы», с заботой о клиенте. 
Твоя цель — помочь клиенту сделать лучший выбор под его вкус и способ заваривания.

Ограничения:
- НЕ даёшь медицинских рекомендаций.
- НЕ обсуждаешь сторонние темы.
- НЕ рассказываешь анекдоты.

Отвечай вежливо, дружелюбно и на Вы. Эмодзи используй минимально. Ответы краткие, структурированные и по делу.
Общайся только на русском языке. Ответ должен быть кратким, профессиональным и без разметки Markdown.`;

async function test() {
  const res = await fetch('https://api.vsegpt.ru/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': 'Bearer ' + process.env.VSEGPT_API_KEY
    },
    body: JSON.stringify({
      model: "openai/gpt-3.5-turbo",
      messages: [
        { role: "system", content: systemPrompt },
        { role: "user", content: "Привет! Какой кофе посоветуешь для турки?" }
      ],
      temperature: 0.3,
      max_tokens: 800
    })
  });
  console.log(res.status);
  console.log(await res.text());
}
test();
